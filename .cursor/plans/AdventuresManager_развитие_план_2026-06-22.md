# План доработки AdventuresManager

Дата: 2026-06-22
Область: `AdventuresManager` (инициализация текущего приключения/сцены, реакция на state, публичное API текущей сцены)

## 1. Цель

Реализовать в `AdventuresManager` устойчивый runtime-поток:
- восстановление текущей точки приключения из сохранения;
- fallback на стартовую точку из настроек, если сохраненная точка отсутствует/невалидна;
- отслеживание изменений текущего приключения/сцены по событиям state;
- публикация события об активной сцене для подписчиков (UI/VM);
- предоставление публичных методов получения текущего контента и доступных действий.

## 2. Текущее состояние (как есть)

- `AdventuresManager` подписан на `AdventureStateLogic.StateChanged`, но пока только логирует событие.
- В `AdventuresStateData` добавлены поля активной runtime-точки: `CurrentAdventureId`, `CurrentAdventureSceneId`.
- В state есть `AdventuresStateData.Adventures[adventureId]` с полями `AdventureId`, `SceneId`, `Parameters` (прогресс отдельного приключения; не путать с `CurrentAdventureSceneId`).
- State-actions для установки активной точки и значения `StateChangeSource` для навигации ещё не реализованы.
- В `RuleSettingsDef` есть `StartAdventure`, но нет отдельного `StartScene`.

## 3. Решения по контракту (утверждение перед кодом)

### 3.1 Где хранить активную точку

Решение зафиксировано:
- активная точка хранится в `AdventuresStateData` в полях:
  - `CurrentAdventureId`
  - `CurrentAdventureSceneId`
- `AdventureStateData.SceneId` в словаре `Adventures` — прогресс конкретного приключения; `CurrentAdventureSceneId` — активная сцена для runtime (`AdventuresManager`).
- `World.Parameters` не используется как указатель текущей точки; это слой глобального прогресса мира (открытые локации, события и т.п.).

### 3.2 Источник стартовой сцены

Вариант A (минимальные изменения, рекомендуется):
- стартовая сцена берется как первая валидная из `AdventureDef.StartScenes`.

Вариант B:
- расширить `RuleSettingsDef` полем `StartScene` и использовать его приоритетно.

## 4. Этапы реализации

## Этап 1. Контракт state для текущей точки

Статус: частично выполнено (поля `CurrentAdventureId` / `CurrentAdventureSceneId` в `AdventuresStateData`).

Задачи:
- ~~добавить хранение активной пары `CurrentAdventureId` / `CurrentAdventureSceneId` в state~~;
- добавить state-action(ы) для установки текущей точки;
- расширить `StateChangeSource` значениями для навигационных изменений.

Ожидаемый результат:
- активная точка меняется только через `StateLogic.ProcessAction(...)`;
- `AdventuresManager` может однозначно читать текущую точку из state.

## Этап 2. Инициализация AdventuresManager

Задачи:
- при `Init()` читать `CurrentAdventureId` / `CurrentAdventureSceneId` из state;
- валидировать adventure/scene по `DefinitionsManager`;
- если точка невалидна или отсутствует:
  - брать `RuleSettings.StartAdventure`;
  - определять стартовую сцену (по п. 3.2);
  - записывать активную точку в state;
- после загрузки состояния публиковать событие об активной сцене.

Ожидаемый результат:
- приложение всегда стартует с валидной активной сцены;
- подписчики получают событие о текущей сцене сразу после инициализации.

## Этап 3. Реакция на изменения state

Задачи:
- в обработчике `StateChanged` сравнивать `CurrentAdventureId` / `CurrentAdventureSceneId` из state с кешем менеджера;
- при фактическом изменении adventure/scene обновлять внутренний runtime-контекст и публиковать событие;
- игнорировать события, не влияющие на активную точку.

Ожидаемый результат:
- нет лишних перезагрузок сцены;
- корректная реакция на переходы, инициированные через state-actions.

## Этап 4. Публичный API AdventuresManager

Задачи:
- добавить методы получения:
  - текущих `CurrentAdventureId` и `CurrentAdventureSceneId`;
  - текущей `SceneData`;
  - текущего контента сцены с фильтрацией ограничений;
  - текущих доступных выборов/действий с фильтрацией ограничений;
- явно разделить:
  - «видимые» элементы;
  - «доступные к выполнению» элементы.

Ожидаемый результат:
- UI/VM получают единый источник данных по активной сцене;
- логика фильтрации ограничений находится в одном месте.

## Этап 5. Событийный контракт для UI

Задачи:
- ввести event в `AdventuresManager` (например, `ActiveSceneChanged`) с payload текущего состояния сцены;
- обеспечить единый формат payload для:
  - первого события после `Init()`;
  - последующих переходов.

Ожидаемый результат:
- подписчики (дальнейшая визуализация) не зависят от внутренней логики менеджера и state.

## Этап 6. Валидация и обработка ошибок

Задачи:
- обработать граничные случаи:
  - отсутствует `RuleSettings`;
  - `StartAdventure` не найден в дефах;
  - нет валидных `StartScenes`;
  - в сохранении ссылка на удаленную/переименованную сцену;
  - частично поврежден блок `Adventures` в state;
- добавить понятные логи ошибок и fallback-стратегии.
- в рамках MVP зафиксировать минимальный безопасный fallback (без детальной продуктовой политики).

Ожидаемый результат:
- менеджер не падает на невалидных данных и может восстановиться в допустимое состояние.

## Этап 7. Тесты

Минимальный набор:
- инициализация из валидного сохранения;
- fallback на стартовую точку из настроек;
- обработка невалидной точки из сохранения;
- событие после `Init()` отправляется один раз и с корректным payload;
- при `StateChanged` событие отправляется только при изменении активной пары;
- фильтрация контента и выборов по ограничениям.

Ожидаемый результат:
- покрыты основные сценарии и регрессии поведения менеджера.

## 5. Порядок внедрения (рекомендуемый)

1. Контракт state + state-actions + `StateChangeSource`.
2. `AdventuresManager.Init()` с восстановлением и fallback.
3. Событие `ActiveSceneChanged`.
4. Публичный API текущей сцены (контент/выборы).
5. Обработка граничных случаев и логирование.
6. Тесты.

## 6. Definition of Done

- `AdventuresManager` после старта всегда имеет валидную активную сцену.
- При изменении активной точки через state-actions подписчики получают событие обновления сцены.
- Публичные методы менеджера возвращают корректно отфильтрованные контент и действия.
- Поведение покрыто тестами на ключевые сценарии.
- Нет ручных правок/создания `.meta` файлов.

## 7. TODO после MVP

- Вернуться к вопросу детальной fallback-политики и описать продуктовые сценарии деградации:
  - порядок источников восстановления точки;
  - поведение при полном отсутствии валидных стартовых данных;
  - правила логирования и пользовательской реакции на невалидный контент.

## Правки по ТЗ

= Сокращения: =
AM/АМ - AdventuresManager - имеющийся класс
RS/РС - RuntimeSceneData - новый класс
Стейт - StateData - класс хранения прогресса Игрока
Приключение, сценарий - дэф приключения (см. DefinitionsManager->Adventures)

= Текущие изменения/отмены в АМ: =
1. Удаляем подписку на _stateLogic

= Краткое изложение концепции: =
АМ внутри себя создает класс-контейнер текущей сцены (РС). АМ теперь нет необходимости отслеживать изменеия в стейте игрока (состоянии Приключения: StateData->AdventuresStateData/InventoryStateData/CharactersStateData) и ретранслирует наружу события из РС (класс: рантайм сцена данные). Отслеживанием стейта и также некоторым изменением стейта (через AdventureStateLogic.ProcessAction()) занимается только РС. Классы, которым нужна информация по Приключению, - подписываются на АМ.

= Описание модели работы: =
1. AdventuresManager (АМ) при инициализации (имеющийся метод Init()) создает внутри себя один экземпляр RuntimeSceneData (РС)
2. АМ подписывается на события РС
3. РС при собственной инициализации (для единообразия - работа с зенджектом через [Inject] private readonly, а все подписки и пр. в методе Init()) подписывается на изменения стейта, (см. AdventureStateLogic.StateChanged). При Dispose() - отписка. Важное уточнение по методу РС.Init() - сначала идет логика инициализации и определения текущей сцены, а только после этого в самом низу Init()-метода выполняется подписка, чтобы избежать двойной загрузки данных друг за другом из-за п.6 и п.7 - когда из-за стейт экшена будет вызвано событие AdventureStateLogic.StateChanged, на корторое подписан сам РС
4. АМ при собственном Dispose() вызывает и Dispose() РС
5. РС смотрит, есть ли в стейте установки на текущие приключение/сценарий и сцену (StateData->Adventures->CurrentAdventureId/CurrentAdventureSceneId). Если есть - РС загружает именно эту сцену этого сценария (об этом подробнее будет далее)
6. если в стейте из п.5 нет информации по текущему прогрессу Приключения, данные берутся из дэфа RuleSettingsDef->StartAdventure. В поле "Приключение" (член РС тип AdventureDef) заносится этот дэф. Затем среди StartScenes выбирается случайная стартовая сцена (обычный System.Random)
7. Если стартовые данные Приключения и Сцены были получены в п.6 - то выполняется один стейт экшен (IStateAction) для изменения сразу двух полей: CurrentAdventureId и CurrentAdventureSceneId
8. После этого идет инициализация членов класса РС (обрати внимание - это всё еще Init() и согласно пояснению в п.3 - подписка на AdventureStateLogic еще не произошла - она в методе Init() будет почти самым последним действием). Итак, чего же должен хранить наш РС? Тут пригодится твой, Глеб, анализ. Но из минималоьного набора, который точно понадобится, я вижу:
    8.1 string идентификатор приключения
	8.2 string идентификатор сцены
	8.3 AdventureDef непосредственно дэф самого приключения
	8.4 События РС (их аналоги должны быть и у АМ - АМ будет подписан на все события и внутри себя и вызывать собственныфе аналоги для своих подписчиков):
	    - event Action<string> ИзменилсяСценарий
		- event Action<string> ИзмениласьСцена
		- event Action ИзменилсяКонтент (речь о доступном из SceneData.Content)
		- event Action ИзменилсяВыбор (речь о доступном из SceneData.Choices)
	8.5 Публичные методы РС (аналоги должны быть у АМ, чтобы АМ мог ретранслировать публичные методы внутреннего класса РС наружу для тех классов, которым это будет необходимо):
	    - List<SceneContentData> GetCurrentContent()
		- List<ChoiceData> GetCurrentChoices()
9. ПРодолжаю описывать логику внутри РС.Init() - после присвоения значения членов идет подписка на всё неробходимое (пока я вижу только необходимость в AdventureStateLogic.StateChanged)
10. После подписки идет вызов события изменения сцены.

ЗАДАНИЕ:
1. проанализируй описание идеи работы AdventuresManager, в случае критических замечаний или уточнений или вопросов - сперва напиши это в чате. После моего ответа уже приступай к следующим пунктам. Также если появятся какие-то уточнения или вопросы в процессе реализации -= можешь остановиться и задать вопросы и дождаться моего ответа.
2. удали из АМ _stateLogic, подписку и методы подписки
3. создай класс RuntimeSceneData (в папке Assets\Modules\RPG\Scripts\Adventure) с необходимыми членами и логикой (см. Описание модели работы)
4. внеси изменения в AdventuresManager (см. Описание модели работы)

//------------------------------------------------

Уточнения Глеба:
Есть 4 критичных уточнения перед реализацией:

Если в StateData.Adventures.CurrentAdventureId/CurrentAdventureSceneId лежат значения, но они невалидны (нет такого adventure или scene в дефах), делаем fallback на RuleSettingsDef.StartAdventure + рандомную стартовую сцену?
Событие AdventureStateLogic.StateChanged в RuntimeSceneData обрабатываем на любой source (и каждый раз перепроверяем текущую сцену), или только для определенных StateChangeSource?
Для шага 7 (одним IStateAction меняем и CurrentAdventureId, и CurrentAdventureSceneId) — ок, если я добавлю новый state action + новый StateChangeSource enum value?
При первичной инициализации RuntimeSceneData.Init() после подписки нужно дернуть только событие ChangedScene, или сразу весь пакет (ChangedAdventure, ChangedScene, ChangedContent, ChangedChoices)?

Мой ответ:
Уточнения по вопросам:
1. Если невалидны и сцена, и приключение - берем стартовое из дэфа (как в п.6 Описание модели работы), если не валидна только сцена - генерим стартовую сцену из дэфа этого приключения (которое указано в стейте)
2. Только для опреджеленных StateChangeSource (возможно стоит использовать Switch (StateChangeSource))
3. Давай всё таки в этом месте будем менать атомарными операциями - то есть два отдельный стейт экшена и два разных StateChangeSource
4. Только ChangedScene. Будт подразумеваться, что это новый запуск игры (не обязательно новый профиль, просто запуск игры) - поэтому все части проекта потом сами опросят и  получат нужные данные для отображения

//------------------------------------------------
